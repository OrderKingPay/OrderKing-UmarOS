import fetch from "node-fetch";

async function run() {
  const HDMASTER_URL = "http://localhost:3000";
  const ADMIN_TOKEN = "supremefounder"; 
  
  // Wait, I can't guarantee HDmaster is running on port 3000 right now.
  console.log("To run this, start HDmaster on port 3000");
}
run();
